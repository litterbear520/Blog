# SkillHub（Skills 页面）实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 新增 `/skills` 自定义页面（导航栏「Skills」入口），以 Hermes 式卡片网格展示常用技能，支持按来源筛选、点击原位展开（介绍 + 安装命令复制 + GitHub 跳转）。

**Architecture:** 数据与组件分离——`src/data/skills.js` 存数据，`src/components/SkillCard/` 是带展开/复制交互的卡片组件，`src/pages/skills.js` 组装 hero + 筛选 + 网格。视觉沿用 `bloglist` 的暖色编辑风调色板与响应式断点。

**Tech Stack:** Docusaurus 3.10.1（v4 模式，ESM 配置）、React 19、CSS Modules、clsx。

**规格文档:** `specs/2026-06-11-skillhub-design.md`

**验证方式说明:** 本项目没有测试框架（package.json 无 test 脚本），按用户全局规范"项目有测试时才加测试"，本计划用 `npm run build`（SSR 构建会执行所有页面代码，能抓出引用错误和 SSR 不安全代码）+ 手动验收清单代替单元测试。

---

## 背景知识（给零上下文的工程师）

- **自定义页面**：`src/pages/*.js` 中的 React 组件自动成为路由（`skills.js` → `/skills`），用 `@theme/Layout` 包裹以获得导航栏和页脚。本站已有同模式页面 `src/pages/bloglist.js` 可对照。
- **CSS Modules**：`*.module.css` 类名局部作用域，`styles.xxx` 引用；`:global([data-theme='dark'])` 用于适配暗色模式。CSS 自定义属性（`--xxx`）会沿 DOM 级联，页面根元素上定义的变量可被子组件的样式引用。
- **调色板**：直接复用 `src/pages/bloglist.module.css` 的变量值（亮色 `#faf9f5`/`#f0eee6` 系，暗色 `#1a1917`/`#262521` 系），保持站点家族感。
- **展开动画**：用 CSS Grid 的 `grid-template-rows: 0fr → 1fr` 过渡实现高度自适应展开，无需 JS 测量高度。注意：被收起的内容外层不能有 padding（padding 不参与收缩），所以详情区是三层结构：`cardDetail`（grid 容器）> `cardDetailInner`（overflow hidden + min-height 0）> `cardDetailContent`（真正的内边距和内容）。
- **构建命令**：`npm run build`（约 1-2 分钟）。构建会对每个页面做 SSR 渲染，顶层使用 `window`/`document` 会直接报错——这就是为什么 clipboard 调用只能放在事件处理器里。

---

### Task 1: 技能数据文件

**Files:**
- Create: `src/data/skills.js`

- [ ] **Step 1: 创建数据文件**

```javascript
// src/data/skills.js
// SkillHub 技能数据。新增技能 = 追加一个对象，无需改组件/页面。
// source 取值：'官方' | '社区' | '自建'，筛选标签按此字段动态生成。
export const SKILLS = [
  {
    id: 'frontend-design',
    name: 'frontend-design',
    author: 'anthropics',
    source: '官方',
    description:
      '创建有独特风格、生产级质量的前端界面，避免千篇一律的 AI 审美。',
    install: [
      {
        label: 'npx',
        command:
          'npx skills add https://github.com/anthropics/skills --skill frontend-design',
      },
    ],
    repo: 'https://github.com/anthropics/skills',
  },
  {
    id: 'superpowers',
    name: 'superpowers',
    author: 'obra',
    source: '社区',
    description:
      '为 Claude Code 提供结构化开发工作流的技能合集：头脑风暴、写计划、TDD、调试等。',
    install: [
      {
        label: 'Claude Code 插件',
        command: '/plugin install superpowers@claude-plugins-official',
      },
    ],
    repo: 'https://github.com/obra/superpowers',
  },
];
```

- [ ] **Step 2: 提交**

```bash
git add src/data/skills.js
git commit -m "SkillHub：新增技能数据文件"
```

---

### Task 2: SkillCard 组件

**Files:**
- Create: `src/components/SkillCard/index.js`
- Create: `src/components/SkillCard/styles.module.css`

- [ ] **Step 1: 创建组件**

```jsx
// src/components/SkillCard/index.js
import React, { useState } from 'react';
import clsx from 'clsx';
import styles from './styles.module.css';

function CopyButton({ command }) {
  // 状态：idle → copied / failed，2 秒后回到 idle
  const [state, setState] = useState('idle');

  const handleCopy = async (e) => {
    e.stopPropagation(); // 不触发卡片收起
    try {
      await navigator.clipboard.writeText(command);
      setState('copied');
    } catch {
      setState('failed');
    }
    setTimeout(() => setState('idle'), 2000);
  };

  return (
    <button className={styles.copyBtn} onClick={handleCopy} type="button">
      {state === 'copied' ? '已复制 ✓' : state === 'failed' ? '复制失败，请手动复制' : '复制'}
    </button>
  );
}

export default function SkillCard({ skill, expanded, onToggle, delay }) {
  return (
    <article
      className={clsx(styles.card, expanded && styles.cardExpanded)}
      style={{ '--delay': `${delay}ms` }}
      onClick={onToggle}
    >
      <h2 className={styles.cardName}>{skill.name}</h2>
      <p className={clsx(styles.cardDesc, !expanded && styles.cardDescClamp)}>
        {skill.description}
      </p>
      <div className={styles.cardMeta}>
        <span>{skill.author}</span>
        <span className={styles.cardBadge}>{skill.source}</span>
        <span className={clsx(styles.cardChevron, expanded && styles.chevronUp)}>
          ▾
        </span>
      </div>

      <div className={styles.cardDetail}>
        <div className={styles.cardDetailInner}>
          <div className={styles.cardDetailContent}>
            {skill.install.map((item) => (
              <div key={item.command}>
                <span className={styles.installLabel}>{item.label}</span>
                <div className={styles.installRow}>
                  <code className={styles.installCommand}>{item.command}</code>
                  <CopyButton command={item.command} />
                </div>
              </div>
            ))}
            <a
              className={styles.repoLink}
              href={skill.repo}
              target="_blank"
              rel="noopener noreferrer"
              onClick={(e) => e.stopPropagation()}
            >
              GitHub 仓库 ↗
            </a>
          </div>
        </div>
      </div>
    </article>
  );
}
```

接口约定（供页面使用）：`skill` 为 `SKILLS` 数组中的一项；`expanded` 布尔值；`onToggle` 点击卡片时调用；`delay` 入场动画延迟毫秒数。组件依赖父级（页面根元素）定义的 CSS 变量（`--card-bg` 等），必须渲染在 `skills.module.css` 的 `.page` 内。

- [ ] **Step 2: 创建组件样式**

```css
/* src/components/SkillCard/styles.module.css */
/* 颜色变量由页面根元素（skills.module.css 的 .page）定义，经级联获得 */

.card {
  background: var(--card-bg);
  border: 1px solid var(--card-border);
  border-radius: 20px;
  padding: 24px;
  cursor: pointer;
  transition:
    transform 0.35s cubic-bezier(0.2, 0.9, 0.3, 1),
    box-shadow 0.35s cubic-bezier(0.2, 0.9, 0.3, 1),
    border-color 0.3s ease;
  animation: fadeSlideUp 0.6s cubic-bezier(0.2, 0.9, 0.3, 1) both;
  animation-delay: var(--delay, 0ms);
}

.card:hover {
  transform: translateY(-4px);
  box-shadow:
    0 16px 40px -8px rgba(20, 20, 19, 0.12),
    0 4px 12px -2px rgba(20, 20, 19, 0.06);
}

:global([data-theme='dark']) .card:hover {
  box-shadow:
    0 16px 40px -8px rgba(0, 0, 0, 0.4),
    0 4px 12px -2px rgba(0, 0, 0, 0.2);
}

.cardExpanded {
  border-color: var(--text-3);
}

@keyframes fadeSlideUp {
  from {
    opacity: 0;
    transform: translateY(28px);
  }
  to {
    opacity: 1;
    transform: translateY(0);
  }
}

.cardName {
  font-family: 'SFMono-Regular', Consolas, 'Liberation Mono', Menlo, monospace;
  font-size: 16px;
  font-weight: 600;
  color: var(--text-1);
  margin: 0 0 10px;
}

.cardDesc {
  font-size: 14px;
  color: var(--text-2);
  line-height: 1.65;
  margin: 0;
}

.cardDescClamp {
  display: -webkit-box;
  -webkit-line-clamp: 2;
  -webkit-box-orient: vertical;
  overflow: hidden;
}

.cardMeta {
  display: flex;
  align-items: center;
  gap: 8px;
  margin-top: 16px;
  font-size: 12px;
  color: var(--text-3);
}

.cardBadge {
  background: var(--tag-bg);
  color: var(--tag-text);
  border-radius: 100px;
  padding: 3px 10px;
}

.cardChevron {
  margin-left: auto;
  font-size: 14px;
  transition: transform 0.3s ease;
}

.chevronUp {
  transform: rotate(180deg);
}

/* ── 展开详情：grid-rows 0fr→1fr 过渡 ── */
.cardDetail {
  display: grid;
  grid-template-rows: 0fr;
  transition: grid-template-rows 0.35s cubic-bezier(0.2, 0.9, 0.3, 1);
}

.cardExpanded .cardDetail {
  grid-template-rows: 1fr;
}

.cardDetailInner {
  overflow: hidden;
  min-height: 0;
}

.cardDetailContent {
  padding-top: 18px;
  display: flex;
  flex-direction: column;
  gap: 14px;
}

.installLabel {
  display: block;
  font-size: 12px;
  color: var(--text-3);
  margin-bottom: 6px;
}

.installRow {
  display: flex;
  align-items: stretch;
  gap: 8px;
}

.installCommand {
  flex: 1;
  font-size: 12px;
  line-height: 1.6;
  padding: 10px 12px;
  border-radius: 8px;
  border: none;
  background: var(--code-bg);
  color: var(--code-text);
  overflow-x: auto;
  white-space: nowrap;
}

.copyBtn {
  font-size: 12px;
  padding: 0 14px;
  border-radius: 8px;
  border: 1px solid var(--card-border);
  background: transparent;
  color: var(--text-2);
  cursor: pointer;
  white-space: nowrap;
  transition: all 0.25s ease;
}

.copyBtn:hover {
  background: var(--card-border);
  color: var(--text-1);
}

.repoLink {
  align-self: flex-start;
  font-size: 13px;
  padding: 8px 18px;
  border-radius: 100px;
  background: var(--filter-active-bg);
  color: var(--filter-active-text) !important;
  text-decoration: none !important;
  transition: opacity 0.25s ease;
}

.repoLink:hover {
  opacity: 0.85;
}
```

- [ ] **Step 3: 提交**

```bash
git add src/components/SkillCard/
git commit -m "SkillHub：新增 SkillCard 卡片组件"
```

---

### Task 3: Skills 页面

**Files:**
- Create: `src/pages/skills.js`
- Create: `src/pages/skills.module.css`

- [ ] **Step 1: 创建页面组件**

```jsx
// src/pages/skills.js
import React, { useState } from 'react';
import Layout from '@theme/Layout';
import Head from '@docusaurus/Head';
import clsx from 'clsx';
import SkillCard from '@site/src/components/SkillCard';
import { SKILLS } from '@site/src/data/skills';
import styles from './skills.module.css';

// 筛选标签从数据动态生成：加入首个 source='自建' 的技能后「自建」自动出现
const SOURCES = ['全部', ...new Set(SKILLS.map((s) => s.source))];

export default function SkillsPage() {
  const [activeSource, setActiveSource] = useState('全部');
  const [expandedId, setExpandedId] = useState(null); // accordion：只记录一个展开 id

  const filtered =
    activeSource === '全部'
      ? SKILLS
      : SKILLS.filter((s) => s.source === activeSource);

  return (
    <Layout title="Skills" description="我常用的和自己创建的 Claude 技能">
      <Head>
        <link
          href="https://fonts.googleapis.com/css2?family=Noto+Serif+SC:wght@400;500;600;700&display=swap"
          rel="stylesheet"
        />
      </Head>

      <div className={styles.page}>
        <header className={styles.hero}>
          <span className={styles.heroLabel}>Skill Hub</span>
          <h1 className={styles.heroTitle}>Skills</h1>
          <p className={styles.heroSubtitle}>我常用的和自己创建的 Claude 技能</p>
          <div className={styles.heroDivider} />
        </header>

        <nav className={styles.filters}>
          {SOURCES.map((src) => (
            <button
              key={src}
              className={clsx(
                styles.filterBtn,
                activeSource === src && styles.filterActive,
              )}
              onClick={() => setActiveSource(src)}
            >
              {src}
            </button>
          ))}
        </nav>

        <section className={styles.grid}>
          {filtered.map((skill, i) => (
            <SkillCard
              key={skill.id}
              skill={skill}
              expanded={expandedId === skill.id}
              onToggle={() =>
                setExpandedId(expandedId === skill.id ? null : skill.id)
              }
              delay={i * 120}
            />
          ))}
        </section>
      </div>
    </Layout>
  );
}
```

- [ ] **Step 2: 创建页面样式**

```css
/* src/pages/skills.module.css */
/* 调色板与 bloglist.module.css 保持一致；新增 --code-bg/--code-text 供安装命令块使用 */
.page {
  --bg: #faf9f5;
  --card-bg: #f0eee6;
  --card-border: #e8e6dc;
  --text-1: #141413;
  --text-2: #5e5d59;
  --text-3: #8a8985;
  --tag-bg: #e8e6dc;
  --tag-text: #5e5d59;
  --filter-bg: transparent;
  --filter-active-bg: #141413;
  --filter-active-text: #faf9f5;
  --code-bg: #141413;
  --code-text: #d6d4ca;

  background: var(--bg);
  min-height: calc(100vh - 60px);
  padding-bottom: 80px;
}

:global([data-theme='dark']) .page {
  --bg: #1a1917;
  --card-bg: #262521;
  --card-border: #3a3935;
  --text-1: #e8e6dc;
  --text-2: #a8a7a2;
  --text-3: #6e6d69;
  --tag-bg: #3a3935;
  --tag-text: #a8a7a2;
  --filter-active-bg: #e8e6dc;
  --filter-active-text: #1a1917;
  --code-bg: #121110;
  --code-text: #c8c6bc;
}

/* ── Hero（与 bloglist 同模式）── */
.hero {
  text-align: center;
  padding: 80px 24px 40px;
  max-width: 640px;
  margin: 0 auto;
}

.heroLabel {
  display: inline-block;
  font-family: 'Noto Serif SC', Georgia, serif;
  font-size: 13px;
  font-weight: 400;
  letter-spacing: 0.15em;
  text-transform: uppercase;
  color: var(--text-3);
  margin-bottom: 16px;
}

.heroTitle {
  font-family: 'Noto Serif SC', Georgia, serif;
  font-size: 48px;
  font-weight: 700;
  color: var(--text-1);
  margin: 0 0 16px;
  letter-spacing: -0.02em;
  line-height: 1.2;
}

.heroSubtitle {
  font-size: 17px;
  color: var(--text-2);
  margin: 0;
  line-height: 1.6;
}

.heroDivider {
  width: 48px;
  height: 2px;
  background: var(--card-border);
  margin: 32px auto 0;
  border-radius: 1px;
}

/* ── 筛选 pills（与 bloglist 同模式）── */
.filters {
  display: flex;
  justify-content: center;
  gap: 8px;
  padding: 0 24px;
  margin-bottom: 48px;
  flex-wrap: wrap;
}

.filterBtn {
  font-family: 'Noto Sans SC', sans-serif;
  font-size: 14px;
  font-weight: 400;
  padding: 8px 20px;
  border-radius: 100px;
  border: 1px solid var(--card-border);
  background: var(--filter-bg);
  color: var(--text-2);
  cursor: pointer;
  transition: all 0.25s ease;
}

.filterBtn:hover {
  background: var(--card-border);
  color: var(--text-1);
}

.filterActive {
  background: var(--filter-active-bg) !important;
  color: var(--filter-active-text) !important;
  border-color: var(--filter-active-bg) !important;
}

/* ── 网格（断点与 bloglist 一致）── */
.grid {
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  align-items: start; /* 展开一张卡片时同行其他卡片不被拉伸 */
  gap: 24px;
  max-width: 1120px;
  margin: 0 auto;
  padding: 0 32px;
}

@media (max-width: 960px) {
  .grid {
    grid-template-columns: repeat(2, 1fr);
  }
}

@media (max-width: 600px) {
  .hero {
    padding: 60px 20px 32px;
  }

  .heroTitle {
    font-size: 36px;
  }

  .grid {
    grid-template-columns: 1fr;
    padding: 0 20px;
    gap: 16px;
  }
}
```

- [ ] **Step 3: 构建验证**

Run: `npm run build`
Expected: 构建成功（输出含 `Generated static files`，无 SSR 报错、无 broken links 报错）。`/skills` 页面此时已可构建，只是导航栏还没有入口。

- [ ] **Step 4: 提交**

```bash
git add src/pages/skills.js src/pages/skills.module.css
git commit -m "SkillHub：新增 /skills 页面（筛选 + 卡片网格）"
```

---

### Task 4: 导航栏入口

**Files:**
- Modify: `docusaurus.config.js:124`（navbar items，「博文」一项之后）

- [ ] **Step 1: 在 navbar 中加入 Skills**

修改 `docusaurus.config.js` 的 `themeConfig.navbar.items`，在 `{ to: '/bloglist', label: '博文', position: 'left' }` 之后插入一项：

```javascript
          { to: '/bloglist', label: '博文', position: 'left' },
          { to: '/skills', label: 'Skills', position: 'left' },
```

- [ ] **Step 2: 构建验证**

Run: `npm run build`
Expected: 构建成功，无 broken links 报错（`onBrokenLinks: 'throw'` 会在链接错误时直接失败）。

- [ ] **Step 3: 提交**

```bash
git add docusaurus.config.js
git commit -m "SkillHub：导航栏新增 Skills 入口"
```

---

### Task 5: 手动验收

**Files:** 无新文件；如发现问题，修复后补充提交。

- [ ] **Step 1: 启动生产预览**

Run: `npm run serve`（已在 Task 4 构建过，无需重复 build）
Expected: 本地 3000 端口可访问。

- [ ] **Step 2: 按验收清单逐项核对**

对照规格 `specs/2026-06-11-skillhub-design.md` 的验收标准：

1. 导航栏出现「Skills」，点击进入 `/skills`；
2. 页面显示 2 张卡片；筛选标签为「全部 / 官方 / 社区」（无「自建」）；点「官方」只剩 frontend-design，点「社区」只剩 superpowers；
3. 点击卡片展开（描述解除两行截断、出现安装命令和 GitHub 按钮、▾ 翻转、边框高亮）；再点收起；展开 A 后点 B，A 自动收起；
4. 复制按钮：点击后剪贴板内容为完整命令、按钮显示「已复制 ✓」约 2 秒（注：`navigator.clipboard` 需要安全上下文，`localhost` 满足）；复制时卡片不收起；
5. GitHub 按钮新标签页打开对应仓库（frontend-design → anthropics/skills，superpowers → obra/superpowers），卡片不收起；
6. 缩放窗口核对三档布局：>960px 三列、600-960px 两列、<600px 单列；
7. 切换亮/暗模式，卡片、徽章、命令块、按钮可读性正常。

Expected: 全部通过。任何一项不通过，修复后重跑 `npm run build` 并重新核对该项。

- [ ] **Step 3: 如有修复则提交**

```bash
git add -A
git commit -m "SkillHub：验收问题修复"
```

（无修复则跳过本步。）
