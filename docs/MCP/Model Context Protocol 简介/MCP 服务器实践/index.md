---
sidebar_position: 2
description: 用 Python SDK 构建第一个带工具的 MCP 服务器，再用内置的检查器（Inspector）在浏览器里测试它。
---

# MCP 服务器实践

用 Python SDK 构建第一个带工具的 MCP 服务器，再用内置的检查器（Inspector）在浏览器里测试它。

import DocCardList from '@theme/DocCardList';

<DocCardList />
