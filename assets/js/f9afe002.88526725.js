"use strict";(self.webpackChunkmy_blog=self.webpackChunkmy_blog||[]).push([["9677"],{9245(e,t,s){s.r(t),s.d(t,{metadata:()=>o,default:()=>d,frontMatter:()=>a,contentTitle:()=>l,toc:()=>p,assets:()=>c});var o=JSON.parse('{"id":"MCP/Model Context Protocol\uFF1A\u9AD8\u7EA7\u4E3B\u9898/\u6838\u5FC3 MCP \u529F\u80FD/roots-walkthrough","title":"Roots \u6F14\u7EC3","description":"\u4EA4\u4E92\u5F0F\u7EC3\u4E60\uFF1A\u5BF9\u7167\u4E00\u4E2A\u5B8C\u6574\u7684 MCP \u804A\u5929\u9879\u76EE\uFF0C\u9010\u6B65\u67E5\u770B\u6839\u76EE\u5F55\u5982\u4F55\u5728\u5BA2\u6237\u7AEF\u5B9A\u4E49\u3001\u5982\u4F55\u88AB\u670D\u52A1\u5668\u8BFB\u53D6\u5E76\u7528\u4E8E\u6388\u6743\u6587\u4EF6\u8BBF\u95EE\u3002","source":"@site/docs/MCP/Model Context Protocol\uFF1A\u9AD8\u7EA7\u4E3B\u9898/\u6838\u5FC3 MCP \u529F\u80FD/06-roots-walkthrough.mdx","sourceDirName":"MCP/Model Context Protocol\uFF1A\u9AD8\u7EA7\u4E3B\u9898/\u6838\u5FC3 MCP \u529F\u80FD","slug":"/MCP/Model Context Protocol\uFF1A\u9AD8\u7EA7\u4E3B\u9898/\u6838\u5FC3 MCP \u529F\u80FD/roots-walkthrough","permalink":"/docs/MCP/Model Context Protocol\uFF1A\u9AD8\u7EA7\u4E3B\u9898/\u6838\u5FC3 MCP \u529F\u80FD/roots-walkthrough","draft":false,"unlisted":false,"tags":[],"version":"current","sidebarPosition":6,"frontMatter":{"description":"\u4EA4\u4E92\u5F0F\u7EC3\u4E60\uFF1A\u5BF9\u7167\u4E00\u4E2A\u5B8C\u6574\u7684 MCP \u804A\u5929\u9879\u76EE\uFF0C\u9010\u6B65\u67E5\u770B\u6839\u76EE\u5F55\u5982\u4F55\u5728\u5BA2\u6237\u7AEF\u5B9A\u4E49\u3001\u5982\u4F55\u88AB\u670D\u52A1\u5668\u8BFB\u53D6\u5E76\u7528\u4E8E\u6388\u6743\u6587\u4EF6\u8BBF\u95EE\u3002"},"sidebar":"tutorialSidebar","previous":{"title":"Roots","permalink":"/docs/MCP/Model Context Protocol\uFF1A\u9AD8\u7EA7\u4E3B\u9898/\u6838\u5FC3 MCP \u529F\u80FD/roots"},"next":{"title":"\u4F20\u8F93\u4E0E\u901A\u4FE1","permalink":"/docs/MCP/Model Context Protocol\uFF1A\u9AD8\u7EA7\u4E3B\u9898/\u4F20\u8F93\u4E0E\u901A\u4FE1/"}}'),n=s(4848),r=s(8453),i=s(3844);let a={description:"\u4EA4\u4E92\u5F0F\u7EC3\u4E60\uFF1A\u5BF9\u7167\u4E00\u4E2A\u5B8C\u6574\u7684 MCP \u804A\u5929\u9879\u76EE\uFF0C\u9010\u6B65\u67E5\u770B\u6839\u76EE\u5F55\u5982\u4F55\u5728\u5BA2\u6237\u7AEF\u5B9A\u4E49\u3001\u5982\u4F55\u88AB\u670D\u52A1\u5668\u8BFB\u53D6\u5E76\u7528\u4E8E\u6388\u6743\u6587\u4EF6\u8BBF\u95EE\u3002"},l="Roots \u6F14\u7EC3",c={},p=[];function m(e){let t={h1:"h1",header:"header",p:"p",...(0,r.R)(),...e.components};return(0,n.jsxs)(n.Fragment,{children:[(0,n.jsx)(t.header,{children:(0,n.jsx)(t.h1,{id:"roots-\u6F14\u7EC3",children:"Roots \u6F14\u7EC3"})}),"\n",(0,n.jsx)(t.p,{children:'\u4EA4\u4E92\u5F0F\u7EC3\u4E60\uFF1A\u5BF9\u7167\u4E0B\u9762\u8FD9\u4E2A\u5B8C\u6574\u7684 MCP \u804A\u5929\u9879\u76EE\uFF0C\u9010\u6B65\u67E5\u770B\u6839\u76EE\u5F55\uFF08roots\uFF09\u5982\u4F55\u5728\u5BA2\u6237\u7AEF\u5B9A\u4E49\u5E76\u901A\u8FC7\u56DE\u8C03\u63D0\u4F9B\u7ED9\u670D\u52A1\u5668\uFF0C\u4EE5\u53CA\u670D\u52A1\u5668\u5982\u4F55\u8BFB\u53D6\u6839\u76EE\u5F55\u5217\u8868\u5E76\u636E\u6B64\u6388\u6743\u6587\u4EF6\u8BBF\u95EE\u3002\u70B9\u51FB"\u4E0B\u4E00\u6B65"\u4F1A\u81EA\u52A8\u6253\u5F00\u5BF9\u5E94\u6587\u4EF6\u5E76\u9AD8\u4EAE\u76F8\u5173\u4EE3\u7801\u3002'}),"\n",(0,n.jsx)(i.A,{variant:"roots"})]})}function d(e={}){let{wrapper:t}={...(0,r.R)(),...e.components};return t?(0,n.jsx)(t,{...e,children:(0,n.jsx)(m,{...e})}):m(e)}},7810(e,t,s){s.d(t,{A:()=>r});var o=s(3941),n=s(1022);function r(){let{prism:e}=(0,n.p)(),{colorMode:t}=(0,o.G)(),s=e.theme,r=e.darkTheme||s;return"dark"===t?r:s}},3844(e,t,s){s.d(t,{A:()=>v});var o=s(4848),n=s(6540),r=s(4164),i=s(1765),a=s(7810);let l={"tour.aria":"\u7B2C {n} \u6B65\uFF0C\u5171 {total} \u6B65","tour.steps.title":"\u6559\u7A0B\u6B65\u9AA4","tour.steps.body":"\u8BA9\u6211\u4EEC\u901A\u8FC7\u4E00\u4E2A\u793A\u4F8B\u9879\u76EE\u6765\u66F4\u597D\u5730\u4E86\u89E3\u5982\u4F55\u5B9E\u73B0\u6B64\u529F\u80FD\u3002","tour.buttons.title":"\u5BFC\u822A\u63A7\u4EF6","tour.buttons.body":"\u4F7F\u7528\u8FD9\u4E9B\u6309\u94AE\u5728\u6559\u7A0B\u6B65\u9AA4\u4E4B\u95F4\u79FB\u52A8\u3002","tour.editor.title":"\u4EE3\u7801\u7F16\u8F91\u5668","tour.editor.body":"\u6211\u4EEC\u5C06\u67E5\u770B\u4E00\u4E2A\u793A\u4F8B\u9879\u76EE\u3002\u6211\u4EEC\u4E0D\u4F1A\u8FD0\u884C\u8FD9\u6BB5\u4EE3\u7801\uFF01\u6211\u4EEC\u53EA\u662F\u770B\u4E00\u770B\u4EE5\u4E86\u89E3\u5B83\u7684\u5DE5\u4F5C\u539F\u7406\u3002","tour.btn.prev":"\u4E0A\u4E00\u6B65","tour.btn.skip":"\u8DF3\u8FC7","tour.btn.next":"\u4E0B\u4E00\u6B65","tour.btn.finish":"\u5B8C\u6210","step.heading":"{n}. {title}","btn.prev":"\u2190 \u4E0A\u4E00\u6B65","btn.next":"\u4E0B\u4E00\u6B65 \u2192","files.heading":"\u6587\u4EF6","aria.openFiles":"\u6253\u5F00\u7684\u6587\u4EF6","aria.closeTab":"\u5173\u95ED {path}","empty.title":"\u6CA1\u6709\u6253\u5F00\u7684\u6587\u4EF6","empty.body":"\u4ECE\u8D44\u6E90\u7BA1\u7406\u5668\u4E2D\u6253\u5F00\u4E00\u4E2A\u6587\u4EF6\uFF0C\u6216\u901A\u8FC7\u6559\u7A0B\u6B65\u9AA4\u8FDB\u884C\u5BFC\u822A"},c={sampling:{files:{".gitignore":`# Python-generated files
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info

# Virtual environments
.venv
.python-version
`,"README.md":`# MCP Logging and Progress Demo

A demonstration of the Model Context Protocol using a STDIO transport.

## Setup

Install dependencies using uv:

\`\`\`bash
uv sync
\`\`\`

## Running the Project

Run the MCP client:

\`\`\`bash
uv run client.py
\`\`\`
`,"client.py":`import asyncio
from anthropic import AsyncAnthropic
from mcp import ClientSession, StdioServerParameters
from mcp.client.stdio import stdio_client
from mcp.client.session import RequestContext
from mcp.types import (
    CreateMessageRequestParams,
    CreateMessageResult,
    TextContent,
    SamplingMessage,
)

anthropic_client = AsyncAnthropic()
model = "claude-sonnet-4-5"

server_params = StdioServerParameters(
    command="uv",
    args=["run", "server.py"],
)


async def chat(input_messages: list[SamplingMessage], max_tokens=4000):
    messages = []
    for msg in input_messages:
        if msg.role == "user" and msg.content.type == "text":
            content = (
                msg.content.text
                if hasattr(msg.content, "text")
                else str(msg.content)
            )
            messages.append({"role": "user", "content": content})
        elif msg.role == "assistant" and msg.content.type == "text":
            content = (
                msg.content.text
                if hasattr(msg.content, "text")
                else str(msg.content)
            )
            messages.append({"role": "assistant", "content": content})

    response = await anthropic_client.messages.create(
        model=model,
        messages=messages,
        max_tokens=max_tokens,
    )

    text = "".join([p.text for p in response.content if p.type == "text"])
    return text


async def sampling_callback(
    context: RequestContext, params: CreateMessageRequestParams
):
    # Call Claude using the Anthropic SDK
    text = await chat(params.messages)

    return CreateMessageResult(
        role="assistant",
        model=model,
        content=TextContent(type="text", text=text),
    )


async def run():
    async with stdio_client(server_params) as (read, write):
        async with ClientSession(
            read, write, sampling_callback=sampling_callback
        ) as session:
            await session.initialize()

            result = await session.call_tool(
                name="summarize",
                arguments={"text_to_summarize": "lots of text"},
            )
            print(result.content)


if __name__ == "__main__":
    import asyncio

    asyncio.run(run())
`,"pyproject.toml":`[project]
name = "sampling"
version = "0.1.0"
description = "Demonstration of sampling with MCP"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "aioconsole>=0.8.1",
    "anthropic>=0.53.0",
    "mcp[cli]>=1.9.3",
]

[tool.setuptools]
py-modules = ["client", "server"]

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"
`,"server.py":`from mcp.server.fastmcp import FastMCP, Context
from mcp.types import SamplingMessage, TextContent

mcp = FastMCP(name="Demo Server")


@mcp.tool()
async def summarize(text_to_summarize: str, ctx: Context):
    prompt = f"""
        Please summarize the following text:
        {text_to_summarize}
    """

    result = await ctx.session.create_message(
        messages=[
            SamplingMessage(
                role="user", content=TextContent(type="text", text=prompt)
            )
        ],
        max_tokens=4000,
        system_prompt="You are a helpful research assistant.",
    )

    if result.content.type == "text":
        return result.content.text
    else:
        raise ValueError("Sampling failed")


if __name__ == "__main__":
    mcp.run(transport="stdio")
`},steps:[{title:"\u542F\u52A8\u91C7\u6837",body:[{type:"p",text:"\u5728\u670D\u52A1\u5668\u7AEF\uFF0C\u5728\u5DE5\u5177\u8C03\u7528\u671F\u95F4\uFF0C\u8FD0\u884C `create_message()` \u65B9\u6CD5\uFF0C\u4F20\u5165\u4E00\u4E9B\u60A8\u5E0C\u671B\u53D1\u9001\u7ED9\u8BED\u8A00\u6A21\u578B\u7684\u6D88\u606F\u3002"}],file:"server.py",line:14,endLine:22},{title:"\u91C7\u6837\u56DE\u8C03",body:[{type:"p",text:"\u5728\u5BA2\u6237\u7AEF\uFF0C\u60A8\u5FC5\u987B\u5B9E\u73B0\u4E00\u4E2A\u91C7\u6837\u56DE\u8C03\u3002\u5B83\u5C06\u63A5\u6536\u670D\u52A1\u5668\u63D0\u4F9B\u7684\u6D88\u606F\u5217\u8868\u3002"}],file:"client.py",line:50,endLine:52},{title:"\u6D88\u606F\u683C\u5F0F",body:[{type:"p",text:"\u670D\u52A1\u5668\u63D0\u4F9B\u7684\u6D88\u606F\u5217\u8868\u662F\u4E3A MCP \u4E2D\u7684\u901A\u4FE1\u800C\u683C\u5F0F\u5316\u7684\u3002\u8FD9\u4E9B\u5355\u72EC\u7684\u6D88\u606F\u4E0D\u4FDD\u8BC1\u4E0E\u60A8\u4F7F\u7528\u7684\u4EFB\u4F55 LLM SDK \u517C\u5BB9\u3002"},{type:"p",text:"\u4F8B\u5982\uFF0C\u5982\u679C\u60A8\u4F7F\u7528\u7684\u662F Anthropic SDK\uFF0C\u60A8\u9700\u8981\u7F16\u5199\u4E00\u4E9B\u8F6C\u6362\u903B\u8F91\uFF0C\u5C06 MCP \u6D88\u606F\u8F6C\u6362\u4E3A\u4E0E Anthropic SDK \u517C\u5BB9\u7684\u683C\u5F0F\u3002"}],file:"client.py",line:24,endLine:38},{title:"\u8FD4\u56DE\u751F\u6210\u7684\u6587\u672C",body:[{type:"p",text:"\u4F7F\u7528 LLM \u751F\u6210\u6587\u672C\u540E\uFF0C\u60A8\u5C06\u8FD4\u56DE\u4E00\u4E2A `CreateMessageResult`\uFF0C\u5176\u4E2D\u5305\u542B\u751F\u6210\u7684\u6587\u672C\u3002"}],file:"client.py",line:53,endLine:60},{title:"\u8FDE\u63A5\u56DE\u8C03",body:[{type:"p",text:"\u522B\u5FD8\u4E86\uFF1A\u5BA2\u6237\u7AEF\u4E0A\u7684\u56DE\u8C03\u9700\u8981\u4F20\u5165 `ClientSession` \u8C03\u7528\u4E2D\u3002"}],file:"client.py",line:66,endLine:66},{title:"\u83B7\u53D6\u7ED3\u679C",body:[{type:"p",text:"\u5BA2\u6237\u7AEF\u751F\u6210\u5E76\u8FD4\u56DE\u4E00\u4E9B\u6587\u672C\u540E\uFF0C\u5B83\u5C06\u88AB\u53D1\u9001\u5230\u670D\u52A1\u5668\u3002\u60A8\u53EF\u4EE5\u5BF9\u8FD9\u6BB5\u6587\u672C\u505A\u4EFB\u4F55\u4E8B\u60C5\uFF1A"},{type:"list",ordered:!1,items:["\u5C06\u5176\u7528\u4F5C\u5DE5\u5177\u4E2D\u5DE5\u4F5C\u6D41\u7A0B\u7684\u4E00\u90E8\u5206","\u51B3\u5B9A\u8FDB\u884C\u53E6\u4E00\u6B21\u91C7\u6837\u8C03\u7528","\u8FD4\u56DE\u751F\u6210\u7684\u6587\u672C"]}],file:"server.py",line:24,endLine:27}]},notifications:{files:{".gitignore":`# Python-generated files
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info

# Virtual environments
.venv
.python-version
`,"README.md":`# MCP Logging and Progress Demo

A demonstration of the Model Context Protocol using a STDIO transport.

## Setup

Install dependencies using uv:

\`\`\`bash
uv sync
\`\`\`

## Running the Project

Run the MCP client:

\`\`\`bash
uv run client.py
\`\`\`
`,"client.py":`from mcp import ClientSession, StdioServerParameters
from mcp.client.stdio import stdio_client
from mcp.types import LoggingMessageNotificationParams

server_params = StdioServerParameters(
    command="uv",
    args=["run", "server.py"],
)


async def logging_callback(params: LoggingMessageNotificationParams):
    print(params.data)


async def print_progress_callback(
    progress: float, total: float | None, message: str | None
):
    if total is not None:
        percentage = (progress / total) * 100
        print(f"Progress: {progress}/{total} ({percentage:.1f}%)")
    else:
        print(f"Progress: {progress}")


async def run():
    async with stdio_client(server_params) as (read, write):
        async with ClientSession(
            read, write, logging_callback=logging_callback
        ) as session:
            await session.initialize()

            await session.call_tool(
                name="add",
                arguments={"a": 1, "b": 3},
                progress_callback=print_progress_callback,
            )


if __name__ == "__main__":
    import asyncio

    asyncio.run(run())
`,"pyproject.toml":`[project]
name = "notifications"
version = "0.1.0"
description = "Demonstration of notifications with MCP"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "aioconsole>=0.8.1",
    "mcp[cli]>=1.9.3",
]

[tool.setuptools]
py-modules = ["client", "server"]

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"
`,"server.py":`from mcp.server.fastmcp import FastMCP, Context
import asyncio

mcp = FastMCP(name="Demo Server")


@mcp.tool()
async def add(a: int, b: int, ctx: Context) -> int:
    await ctx.info("Preparing to add...")
    await ctx.report_progress(20, 100)

    await asyncio.sleep(2)

    await ctx.info("OK, adding...")
    await ctx.report_progress(80, 100)

    return a + b


if __name__ == "__main__":
    mcp.run(transport="stdio")
`},steps:[{title:"\u5DE5\u5177\u51FD\u6570\u63A5\u6536 Context \u53C2\u6570",body:[{type:"p",text:"\u5DE5\u5177\u51FD\u6570\u4F1A\u81EA\u52A8\u63A5\u6536 'Context' \u4F5C\u4E3A\u5176\u6700\u540E\u4E00\u4E2A\u53C2\u6570\u3002\u8FD9\u4E2A\u5BF9\u8C61\u5177\u6709\u7528\u4E8E\u5411\u5BA2\u6237\u7AEF\u8BB0\u5F55\u65E5\u5FD7\u548C\u62A5\u544A\u8FDB\u5EA6\u7684\u65B9\u6CD5\u3002"}],file:"server.py",line:8,endLine:8},{title:"\u4F7F\u7528 context \u521B\u5EFA\u65E5\u5FD7\u548C\u8FDB\u5EA6",body:[{type:"p",text:"\u5728\u60A8\u7684\u5DE5\u5177\u51FD\u6570\u4E2D\uFF0C\u8C03\u7528 `info()`\u3001`warning()`\u3001`debug()` \u6216 `error()` \u65B9\u6CD5\u6765\u4E3A\u5BA2\u6237\u7AEF\u8BB0\u5F55\u4E0D\u540C\u7C7B\u578B\u7684\u6D88\u606F\u3002\u8FD8\u8981\u8C03\u7528 `report_progress()` \u65B9\u6CD5\u6765\u4F30\u8BA1\u5DE5\u5177\u8C03\u7528\u5269\u4F59\u5DE5\u4F5C\u91CF\u3002"}],file:"server.py",line:9,endLine:15},{title:"\u5728\u5BA2\u6237\u7AEF\u5B9A\u4E49\u56DE\u8C03",body:[{type:"p",text:"\u5BA2\u6237\u7AEF\u9700\u8981\u5B9A\u4E49\u65E5\u5FD7\u8BB0\u5F55\u548C\u8FDB\u5EA6\u56DE\u8C03\uFF0C\u8FD9\u4E9B\u56DE\u8C03\u5C06\u5728\u670D\u52A1\u5668\u53D1\u51FA\u65E5\u5FD7\u6216\u8FDB\u5EA6\u6D88\u606F\u65F6\u81EA\u52A8\u88AB\u8C03\u7528\u3002\u8FD9\u4E9B\u56DE\u8C03\u5E94\u5C1D\u8BD5\u5411\u7528\u6237\u663E\u793A\u63D0\u4F9B\u7684\u65E5\u5FD7\u8BB0\u5F55\u548C\u8FDB\u5EA6\u6570\u636E\u3002"}],file:"client.py",line:11,endLine:22},{title:"\u5C06\u56DE\u8C03\u4F20\u9012\u7ED9\u76F8\u5E94\u7684\u51FD\u6570",body:[{type:"p",text:"\u786E\u4FDD\u60A8\u5C06\u65E5\u5FD7\u8BB0\u5F55\u56DE\u8C03\u63D0\u4F9B\u7ED9 `ClientSession`\uFF0C\u5E76\u5C06\u8FDB\u5EA6\u56DE\u8C03\u63D0\u4F9B\u7ED9 `call_tool()` \u51FD\u6570\u3002"}],file:"client.py",line:27,endLine:36}]},roots:{files:{".env.example":`CLAUDE_MODEL="claude-sonnet-4-5"
ANTHROPIC_API_KEY=""`,".gitignore":`.env
__pycache__
.venv
.DS_Store`,"README.md":`# MCP Chat with File System Access

MCP Chat is a command-line interface application that enables interactive chat capabilities with AI models through the Anthropic API. The application supports file system operations with controlled access to specified directories, video conversion capabilities, and extensible tool integrations via the MCP (Model Control Protocol) architecture.

## Prerequisites

- Python 3.10+
- Anthropic API Key
- FFmpeg (for video conversion features)

## Setup

_You must have FFmpeg already installed to convert a video file_. To install FFmpeg on MacOS run:

\`\`\`
brew install ffmpeg
\`\`\`

### Step 1: Configure the environment variables

1. Copy the \`.env.example\` file to create a new \`.env\` file:

\`\`\`bash
cp .env.example .env
\`\`\`

2. Edit the \`.env\` file and set your environment variables:

\`\`\`
CLAUDE_MODEL="claude-sonnet-4-5"  # Or your preferred Claude model
ANTHROPIC_API_KEY=""  # Enter your Anthropic API secret key
\`\`\`

### Step 2: Install dependencies

#### Setup with uv

[uv](https://github.com/astral-sh/uv) is a fast Python package installer and resolver.

1. Install uv, if not already installed:

\`\`\`bash
pip install uv
\`\`\`

2. Install dependencies:

\`\`\`bash
uv sync
\`\`\`

3. Run the project

When running the project, you must specify one or more root directories that the MCP server will have access to. Only files and directories within these roots can be accessed by the server.

\`\`\`bash
uv run main.py <root1> [root2] [root3] ...
\`\`\`

Examples:

\`\`\`bash
# Single directory
uv run main.py /path/to/videos

# Multiple directories
uv run main.py /home/user/videos /mnt/storage/media ~/Documents

# Current directory
uv run main.py .
\`\`\`

## Features

### File System Access

The server can only access files and directories within the specified root paths. This provides security by limiting file system access to approved locations.

### Available Tools

- **list_roots**: List all accessible root directories
- **read_dir**: Read contents of a directory (must be within a root)
- **convert_video**: Convert MP4 videos to other formats (avi, mov, webm, mkv, gif)

### Video Conversion

The video conversion tool uses FFmpeg to convert MP4 files to various formats:

- Standard video formats: AVI, MOV, WebM, MKV
- GIF conversion with optimized settings
- Medium quality preset for balanced file size and quality
`,"core/__init__.py":"","core/chat.py":`from core.claude import Claude
from mcp_client import MCPClient
from core.tools import ToolManager
from anthropic.types import MessageParam


class Chat:
    def __init__(self, claude_service: Claude, clients: dict[str, MCPClient]):
        self.claude_service: Claude = claude_service
        self.clients: dict[str, MCPClient] = clients
        self.messages: list[MessageParam] = []

    async def _process_query(self, query: str):
        self.messages.append({"role": "user", "content": query})

    async def run(
        self,
        query: str,
        stream: bool = False,
        on_event=None,
    ) -> str:
        final_text_response = ""

        await self._process_query(query)

        while True:
            if stream and on_event:
                response = await self.claude_service.chat_stream(
                    messages=self.messages,
                    tools=await ToolManager.get_all_tools(self.clients),
                    on_event=on_event,
                )
            else:
                response = await self.claude_service.chat(
                    messages=self.messages,
                    tools=await ToolManager.get_all_tools(self.clients),
                )

            self.claude_service.add_assistant_message(self.messages, response)

            if response.stop_reason == "tool_use":
                if not stream:
                    print(self.claude_service.text_from_message(response))
                tool_result_parts = await ToolManager.execute_tool_requests(
                    self.clients, response
                )

                self.claude_service.add_user_message(
                    self.messages, tool_result_parts
                )
            else:
                final_text_response = self.claude_service.text_from_message(
                    response
                )
                break

        return final_text_response
`,"core/claude.py":`from anthropic import AsyncAnthropic
from anthropic.types import Message


class Claude:
    def __init__(self, model: str):
        self.client = AsyncAnthropic()
        self.model = model

    def add_user_message(self, messages: list, message):
        user_message = {
            "role": "user",
            "content": message.content
            if isinstance(message, Message)
            else message,
        }
        messages.append(user_message)

    def add_assistant_message(self, messages: list, message):
        assistant_message = {
            "role": "assistant",
            "content": message.content
            if isinstance(message, Message)
            else message,
        }
        messages.append(assistant_message)

    def text_from_message(self, message: Message):
        return "
".join(
            [block.text for block in message.content if block.type == "text"]
        )

    async def chat(
        self,
        messages,
        system=None,
        temperature=1.0,
        stop_sequences=[],
        tools=None,
        thinking=False,
        thinking_budget=1024,
    ) -> Message:
        params = {
            "model": self.model,
            "max_tokens": 8000,
            "messages": messages,
            "temperature": temperature,
            "stop_sequences": stop_sequences,
        }

        if thinking:
            params["thinking"] = {
                "type": "enabled",
                "budget_tokens": thinking_budget,
            }

        if tools:
            params["tools"] = tools

        if system:
            params["system"] = system

        message = await self.client.messages.create(**params)
        return message

    async def chat_stream(
        self,
        messages,
        system=None,
        temperature=1.0,
        stop_sequences=[],
        tools=None,
        thinking=False,
        thinking_budget=1024,
        on_event=None,
    ) -> Message:
        params = {
            "model": self.model,
            "max_tokens": 8000,
            "messages": messages,
            "temperature": temperature,
            "stop_sequences": stop_sequences,
        }

        if thinking:
            params["thinking"] = {
                "type": "enabled",
                "budget_tokens": thinking_budget,
            }

        if tools:
            params["tools"] = tools

        if system:
            params["system"] = system

        async with self.client.messages.stream(**params) as stream:
            if on_event:
                async for event in stream:
                    await on_event(event)
            else:
                async for event in stream:
                    pass

        return await stream.get_final_message()
`,"core/cli.py":`from prompt_toolkit import PromptSession
from prompt_toolkit.styles import Style
from prompt_toolkit.history import InMemoryHistory
from core.cli_chat import CliChat
import json
from pyboxen import boxen


class CliApp:
    def __init__(self, agent: CliChat):
        self.agent = agent
        self.history = InMemoryHistory()
        self.session = PromptSession(
            history=self.history,
            style=Style.from_dict(
                {
                    "prompt": "#aaaaaa",
                    "completion-menu.completion": "bg:#222222 #ffffff",
                    "completion-menu.completion.current": "bg:#444444 #ffffff",
                }
            ),
            complete_while_typing=True,
            complete_in_thread=True,
        )

    async def initialize(self):
        pass

    async def run(self):
        while True:
            try:
                user_input = await self.session.prompt_async("> ")
                if not user_input.strip():
                    continue

                print()

                tool_calls = {}
                response_text = ""

                async def handle_event(event):
                    nonlocal response_text
                    if hasattr(event, "type"):
                        if event.type == "content_block_delta":
                            if hasattr(event, "delta") and hasattr(
                                event.delta, "type"
                            ):
                                if event.delta.type == "text_delta":
                                    response_text += event.delta.text
                                    print(event.delta.text, end="", flush=True)
                                elif event.delta.type == "input_json_delta":
                                    # Track tool call arguments as they stream
                                    index = event.index
                                    if index not in tool_calls:
                                        tool_calls[index] = {
                                            "name": "",
                                            "args": "",
                                        }
                                    tool_calls[index]["args"] += (
                                        event.delta.partial_json
                                    )
                        elif event.type == "content_block_start":
                            if hasattr(event, "content_block") and hasattr(
                                event.content_block, "type"
                            ):
                                if event.content_block.type == "tool_use":
                                    print()  # New line before tool call
                                    # Store tool name but don't print yet
                                    index = getattr(event, "index", 0)
                                    if index not in tool_calls:
                                        tool_calls[index] = {
                                            "name": "",
                                            "args": "",
                                        }
                                    tool_calls[index]["name"] = (
                                        event.content_block.name
                                    )
                        elif event.type == "content_block_stop":
                            if event.index in tool_calls:
                                tool_name = tool_calls[event.index]["name"]
                                args_json = tool_calls[event.index]["args"]

                                try:
                                    parsed_args = json.loads(args_json)
                                    formatted_args = json.dumps(
                                        parsed_args, indent=2
                                    )
                                    tool_content = f"\u{1F527} {tool_name}

Arguments:
{formatted_args}"
                                except (
                                    json.JSONDecodeError,
                                    TypeError,
                                    ValueError,
                                ):
                                    tool_content = f"\u{1F527} {tool_name}

Arguments: {args_json}"

                                tool_box = boxen(
                                    tool_content,
                                    title="Tool Call",
                                    style="rounded",
                                    color="blue",
                                    padding=0,
                                )
                                print(tool_box)
                                del tool_calls[event.index]

                await self.agent.run(
                    user_input, stream=True, on_event=handle_event
                )

                print()  # Add newline after everything

            except KeyboardInterrupt:
                break
`,"core/cli_chat.py":`from typing import List
from mcp.types import Prompt, PromptMessage
from anthropic.types import MessageParam

from core.chat import Chat
from core.claude import Claude
from mcp_client import MCPClient


class CliChat(Chat):
    def __init__(
        self,
        doc_client: MCPClient,
        clients: dict[str, MCPClient],
        claude_service: Claude,
    ):
        super().__init__(clients=clients, claude_service=claude_service)

        self.doc_client: MCPClient = doc_client

    async def list_prompts(self) -> list[Prompt]:
        return await self.doc_client.list_prompts()

    async def get_prompt(
        self, command: str, doc_id: str
    ) -> list[PromptMessage]:
        return await self.doc_client.get_prompt(command, {"doc_id": doc_id})

    async def _process_query(self, query: str):
        self.messages.append({"role": "user", "content": query})


def convert_prompt_message_to_message_param(
    prompt_message: "PromptMessage",
) -> MessageParam:
    role = "user" if prompt_message.role == "user" else "assistant"

    content = prompt_message.content

    # Check if content is a dict-like object with a "type" field
    if isinstance(content, dict) or hasattr(content, "__dict__"):
        content_type = (
            content.get("type", None)
            if isinstance(content, dict)
            else getattr(content, "type", None)
        )
        if content_type == "text":
            content_text = (
                content.get("text", "")
                if isinstance(content, dict)
                else getattr(content, "text", "")
            )
            return {"role": role, "content": content_text}

    if isinstance(content, list):
        text_blocks = []
        for item in content:
            # Check if item is a dict-like object with a "type" field
            if isinstance(item, dict) or hasattr(item, "__dict__"):
                item_type = (
                    item.get("type", None)
                    if isinstance(item, dict)
                    else getattr(item, "type", None)
                )
                if item_type == "text":
                    item_text = (
                        item.get("text", "")
                        if isinstance(item, dict)
                        else getattr(item, "text", "")
                    )
                    text_blocks.append({"type": "text", "text": item_text})

        if text_blocks:
            return {"role": role, "content": text_blocks}

    return {"role": role, "content": ""}


def convert_prompt_messages_to_message_params(
    prompt_messages: List[PromptMessage],
) -> List[MessageParam]:
    return [
        convert_prompt_message_to_message_param(msg) for msg in prompt_messages
    ]
`,"core/tools.py":`import json
from typing import Optional, Literal, List
from mcp.types import CallToolResult, Tool, TextContent
from mcp_client import MCPClient
from anthropic.types import Message, ToolResultBlockParam


class ToolManager:
    @classmethod
    async def get_all_tools(cls, clients: dict[str, MCPClient]) -> list[Tool]:
        """Gets all tools from the provided clients."""
        tools = []
        for client in clients.values():
            tool_models = await client.list_tools()
            tools += [
                {
                    "name": t.name,
                    "description": t.description,
                    "input_schema": t.inputSchema,
                }
                for t in tool_models
            ]
        return tools

    @classmethod
    async def _find_client_with_tool(
        cls, clients: list[MCPClient], tool_name: str
    ) -> Optional[MCPClient]:
        """Finds the first client that has the specified tool."""
        for client in clients:
            tools = await client.list_tools()
            tool = next((t for t in tools if t.name == tool_name), None)
            if tool:
                return client
        return None

    @classmethod
    def _build_tool_result_part(
        cls,
        tool_use_id: str,
        text: str,
        status: Literal["success"] | Literal["error"],
    ) -> ToolResultBlockParam:
        """Builds a tool result part dictionary."""
        return {
            "tool_use_id": tool_use_id,
            "type": "tool_result",
            "content": text,
            "is_error": status == "error",
        }

    @classmethod
    async def execute_tool_requests(
        cls, clients: dict[str, MCPClient], message: Message
    ) -> List[ToolResultBlockParam]:
        """Executes a list of tool requests against the provided clients."""
        tool_requests = [
            block for block in message.content if block.type == "tool_use"
        ]
        tool_result_blocks: list[ToolResultBlockParam] = []
        for tool_request in tool_requests:
            tool_use_id = tool_request.id
            tool_name = tool_request.name
            tool_input = tool_request.input

            client = await cls._find_client_with_tool(
                list(clients.values()), tool_name
            )

            if not client:
                tool_result_part = cls._build_tool_result_part(
                    tool_use_id, "Could not find that tool", "error"
                )
                tool_result_blocks.append(tool_result_part)
                continue

            tool_output = None
            try:
                tool_output: CallToolResult | None = await client.call_tool(
                    tool_name, tool_input
                )
                items = []
                if tool_output:
                    items = tool_output.content
                content_list = [
                    item.text for item in items if isinstance(item, TextContent)
                ]
                content_json = json.dumps(content_list)
                tool_result_part = cls._build_tool_result_part(
                    tool_use_id,
                    content_json,
                    "error"
                    if tool_output and tool_output.isError
                    else "success",
                )
            except Exception as e:
                error_message = f"Error executing tool '{tool_name}': {e}"
                print(error_message)
                tool_result_part = cls._build_tool_result_part(
                    tool_use_id,
                    json.dumps({"error": error_message}),
                    "error"
                    if tool_output and tool_output.isError
                    else "success",
                )

            tool_result_blocks.append(tool_result_part)
        return tool_result_blocks
`,"core/utils.py":`from pathlib import Path
from urllib.parse import unquote, urlparse


def file_url_to_path(file_url) -> Path:
    """Convert a file:// URL to a Path object."""
    url_str = str(file_url)
    parsed = urlparse(url_str)
    path = unquote(parsed.path)
    if len(path) > 2 and path[0] == "/" and path[2] == ":":
        path = path[1:]

    return Path(path)`,"core/video_converter.py":`import os
import asyncio
from pathlib import Path


class VideoConverter:
    """Handles video conversion operations using ffmpeg."""
    
    # Quality presets for video conversion
    QUALITY_PRESETS = {
        "low": {"crf": "28", "preset": "fast"},
        "medium": {"crf": "23", "preset": "medium"},
        "high": {"crf": "18", "preset": "slow"},
    }
    
    SUPPORTED_FORMATS = ["webm", "mkv", "avi", "mov", "gif"]
    
    @classmethod
    def validate_input(cls, input_path: str) -> Path:
        """Validate the input file exists and is an MP4."""
        input_file = Path(input_path)
        
        if not input_file.exists():
            raise ValueError(f"Input file not found: {input_path}")
        
        if not input_path.lower().endswith(".mp4"):
            raise ValueError("Input file must be an MP4 file")
            
        return input_file
    
    @classmethod
    def generate_output_path(cls, input_path: str, format: str) -> str:
        """Generate output path by replacing the file extension."""
        base_path = os.path.splitext(input_path)[0]
        return f"{base_path}.{format.lower()}"
    
    @classmethod
    def build_ffmpeg_command(cls, input_path: str, output_path: str, format: str) -> list:
        """Build the ffmpeg command based on format settings."""
        preset = cls.QUALITY_PRESETS["medium"]
        
        # Base command
        cmd = ["ffmpeg", "-i", input_path, "-y"]
        
        if format.lower() == "gif":
            # Special handling for GIF conversion
            cmd.extend([
                "-vf", "fps=15,scale=480:-1:flags=lanczos",
                "-c:v", "gif",
                output_path
            ])
        elif format.lower() in cls.SUPPORTED_FORMATS:
            # Standard video conversion
            cmd.extend([
                "-c:v", "libx264",
                "-preset", preset["preset"],
                "-crf", preset["crf"],
                "-c:a", "aac",
                "-b:a", "128k",
                output_path
            ])
        else:
            raise ValueError(f"Unsupported output format: {format}")
            
        return cmd
    
    @classmethod
    async def convert(cls, input_path: str, format: str) -> str:
        """
        Convert video file to specified format.
        Returns success message or raises an error.
        """
        # Validate input
        cls.validate_input(input_path)
        
        # Generate output path
        output_path = cls.generate_output_path(input_path, format)
        
        # Build ffmpeg command
        cmd = cls.build_ffmpeg_command(input_path, output_path, format)
        
        try:
            # Run ffmpeg asynchronously
            process = await asyncio.create_subprocess_exec(
                *cmd,
                stdout=asyncio.subprocess.PIPE,
                stderr=asyncio.subprocess.PIPE
            )
            _, stderr = await process.communicate()
            
            if process.returncode != 0:
                raise RuntimeError(f"FFmpeg conversion failed: {stderr.decode()}")
                
            return f"Successfully converted {input_path} to {output_path}"
            
        except FileNotFoundError:
            raise RuntimeError("FFmpeg not found. Please ensure ffmpeg is installed and in PATH")`,"main.py":`import asyncio
import sys
import os
from dotenv import load_dotenv
from contextlib import AsyncExitStack

from mcp_client import MCPClient
from core.claude import Claude

from core.cli_chat import CliChat
from core.cli import CliApp

load_dotenv()

# Anthropic Config
claude_model = os.getenv("CLAUDE_MODEL", "claude-sonnet-4-5")
anthropic_api_key = os.getenv("ANTHROPIC_API_KEY", "")


assert claude_model, "Error: CLAUDE_MODEL cannot be empty. Update .env"
assert anthropic_api_key, (
    "Error: ANTHROPIC_API_KEY cannot be empty. Update .env"
)


async def main():
    claude_service = Claude(model=claude_model)

    # Get root directories from command line arguments
    root_paths = sys.argv[1:]
    if not root_paths:
        print("Usage: uv run main.py <root1> [root2] ...")
        print("Example: uv run main.py /path/to/videos /another/path")
        sys.exit(1)

    clients = {}

    async with AsyncExitStack() as stack:
        # Create the MCP client with the provided root directories
        doc_client = await stack.enter_async_context(
            MCPClient(
                command="uv", args=["run", "mcp_server.py"], roots=root_paths
            )
        )
        clients["doc_client"] = doc_client

        chat = CliChat(
            doc_client=doc_client,
            clients=clients,
            claude_service=claude_service,
        )

        cli = CliApp(chat)
        await cli.initialize()
        await cli.run()


if __name__ == "__main__":
    if sys.platform == "win32":
        asyncio.set_event_loop_policy(asyncio.WindowsProactorEventLoopPolicy())
    asyncio.run(main())
`,"mcp_client.py":`from typing import Optional, Any
from contextlib import AsyncExitStack
from mcp import ClientSession, StdioServerParameters, types
from mcp.client.stdio import stdio_client
from mcp.types import Root, ListRootsResult, ErrorData
from mcp.shared.context import RequestContext
from pathlib import Path
from pydantic import FileUrl

import json
from pydantic import AnyUrl


class MCPClient:
    def __init__(
        self,
        command: str,
        args: list[str],
        env: Optional[dict] = None,
        roots: Optional[list[str]] = None,
    ):
        self._command = command
        self._args = args
        self._env = env
        self._roots = self._create_roots(roots) if roots else []
        self._session: Optional[ClientSession] = None
        self._exit_stack: AsyncExitStack = AsyncExitStack()

    def _create_roots(self, root_paths: list[str]) -> list[Root]:
        """Convert path strings to Root objects."""
        roots = []
        for path in root_paths:
            p = Path(path).resolve()
            file_url = FileUrl(f"file://{p}")
            roots.append(Root(uri=file_url, name=p.name or "Root"))
        return roots

    async def _handle_list_roots(
        self, context: RequestContext["ClientSession", None]
    ) -> ListRootsResult | ErrorData:
        """Callback for when server requests roots."""
        return ListRootsResult(roots=self._roots)

    async def connect(self):
        server_params = StdioServerParameters(
            command=self._command,
            args=self._args,
            env=self._env,
        )
        stdio_transport = await self._exit_stack.enter_async_context(
            stdio_client(server_params)
        )
        _stdio, _write = stdio_transport
        self._session = await self._exit_stack.enter_async_context(
            ClientSession(
                _stdio,
                _write,
                list_roots_callback=self._handle_list_roots
                if self._roots
                else None,
            )
        )
        await self._session.initialize()

    def session(self) -> ClientSession:
        if self._session is None:
            raise ConnectionError(
                "Client session not initialized or cache not populated. Call connect_to_server first."
            )
        return self._session

    async def list_tools(self) -> list[types.Tool]:
        result = await self.session().list_tools()
        return result.tools

    async def call_tool(
        self, tool_name: str, tool_input
    ) -> types.CallToolResult | None:
        return await self.session().call_tool(tool_name, tool_input)

    async def list_prompts(self) -> list[types.Prompt]:
        result = await self.session().list_prompts()
        return result.prompts

    async def get_prompt(self, prompt_name, args: dict[str, str]):
        result = await self.session().get_prompt(prompt_name, args)
        return result.messages

    async def read_resource(self, uri: str) -> Any:
        result = await self.session().read_resource(AnyUrl(uri))
        resource = result.contents[0]

        if isinstance(resource, types.TextResourceContents):
            if resource.mimeType == "application/json":
                return json.loads(resource.text)

            return resource.text

    async def cleanup(self):
        await self._exit_stack.aclose()
        self._session = None

    async def __aenter__(self):
        await self.connect()
        return self

    async def __aexit__(self, exc_type, exc_val, exc_tb):
        await self.cleanup()
`,"mcp_server.py":`from pathlib import Path
from mcp.server.fastmcp import FastMCP
from pydantic import Field
from mcp.server.fastmcp import Context
from core.video_converter import VideoConverter
from core.utils import file_url_to_path

mcp = FastMCP("VidsMCP", log_level="ERROR")


async def is_path_allowed(requested_path: Path, ctx: Context) -> bool:
    roots_result = await ctx.session.list_roots()
    client_roots = roots_result.roots

    if not requested_path.exists():
        return False

    if requested_path.is_file():
        requested_path = requested_path.parent

    for root in client_roots:
        root_path = file_url_to_path(root.uri)
        try:
            requested_path.relative_to(root_path)
            return True
        except ValueError:
            continue

    return False


@mcp.tool()
async def convert_video(
    input_path: str = Field(description="Path to the input MP4 file"),
    format: str = Field(description="Output format (e.g. 'mov')"),
    *,
    ctx: Context,
):
    """Convert an MP4 video file to another format using ffmpeg"""
    input_file = VideoConverter.validate_input(input_path)

    # Ensure the input file is contained in a root
    if not await is_path_allowed(input_file, ctx):
        raise ValueError(f"Access to path is not allowed: {input_path}")

    return await VideoConverter.convert(input_path, format)


@mcp.tool()
async def list_roots(ctx: Context):
    """
    List all directories that are accessible to this server.
    These are the root directories where files can be read from or written to.
    """
    roots_result = await ctx.session.list_roots()
    client_roots = roots_result.roots

    return [file_url_to_path(root.uri) for root in client_roots]


@mcp.tool()
async def read_dir(
    path: str = Field(description="Path to a directory to read"),
    *,
    ctx: Context,
):
    """Read directory contents. Path must be within one of the client's roots."""
    requested_path = Path(path).resolve()

    if not await is_path_allowed(requested_path, ctx):
        raise ValueError("Error: can only read directories within a root")

    return [entry.name for entry in requested_path.iterdir()]


if __name__ == "__main__":
    mcp.run(transport="stdio")
`,"pyproject.toml":`[project]
name = "app"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "anthropic>=0.51.0",
    "mcp[cli]>=1.8.0",
    "prompt-toolkit>=3.0.51",
    "pyboxen>=1.3.0",
    "python-dotenv>=1.1.0",
]
`},steps:[{title:"\u5B9A\u4E49\u6839\u76EE\u5F55",body:[{type:"p",text:"\u7406\u60F3\u60C5\u51B5\u4E0B\uFF0C\u7528\u6237\u5C06\u6307\u5B9A MCP \u670D\u52A1\u5668\u53EF\u4EE5\u8BBF\u95EE\u54EA\u4E9B\u6587\u4EF6/\u6587\u4EF6\u5939\u3002"},{type:"p",text:"\u8BE5\u7A0B\u5E8F\u8BBE\u7F6E\u4E3A\u63A5\u53D7\u4E00\u4E2A CLI \u53C2\u6570\u5217\u8868\uFF0C\u8FD9\u4E9B\u53C2\u6570\u88AB\u89E3\u91CA\u4E3A\u7528\u6237\u5E0C\u671B\u5141\u8BB8\u8BBF\u95EE\u7684\u8DEF\u5F84\u3002"},{type:"p",text:"\u8BE5\u8DEF\u5F84\u5217\u8868\u5728\u7B2C 42 \u884C\u63D0\u4F9B\u7ED9 `MCPClient`\u3002"}],file:"main.py",line:30,endLine:30},{title:"\u521B\u5EFA\u6839\u5BF9\u8C61",body:[{type:"p",text:"\u6839\u636E MCP \u89C4\u8303\uFF0C\u6240\u6709\u6839\u76EE\u5F55\u90FD\u5E94\u5177\u6709\u4EE5 `file://` \u5F00\u5934\u7684 URI\u3002"},{type:"p",text:"\u6B64\u51FD\u6570\u83B7\u53D6\u7528\u6237\u63D0\u4F9B\u7684\u8DEF\u5F84\u5217\u8868\uFF0C\u5E76\u5C06\u5B83\u4EEC\u8F6C\u6362\u4E3A `Root` \u5BF9\u8C61\u3002"}],file:"mcp_client.py",line:29,endLine:36},{title:"\u6839\u76EE\u5F55\u56DE\u8C03",body:[{type:"p",text:"\u5BA2\u6237\u7AEF\u4E0D\u4F1A\u7ACB\u5373\u5C06\u6839\u76EE\u5F55\u5217\u8868\u63D0\u4F9B\u7ED9\u670D\u52A1\u5668\u3002\u76F8\u53CD\uFF0C\u670D\u52A1\u5668\u53EF\u4EE5\u5728\u672A\u6765\u67D0\u4E2A\u65F6\u95F4\u70B9\u5411\u5BA2\u6237\u7AEF\u53D1\u51FA\u8BF7\u6C42\u3002\u6211\u4EEC\u521B\u5EFA\u4E00\u4E2A\u56DE\u8C03\uFF0C\u5F53\u670D\u52A1\u5668\u8BF7\u6C42\u6839\u76EE\u5F55\u65F6\u5C06\u6267\u884C\u8BE5\u56DE\u8C03\u3002\u8BE5\u56DE\u8C03\u9700\u8981\u5728 `ListRootsResult` \u5BF9\u8C61\u4E2D\u8FD4\u56DE\u6839\u76EE\u5F55\u5217\u8868\u3002"},{type:"p",text:"\u6B64\u56DE\u8C03\u5728\u7B2C 58 \u884C\u4F20\u5165 ClientSession\u3002"}],file:"mcp_client.py",line:38,endLine:42},{title:"\u4F7F\u7528\u6839\u76EE\u5F55",body:[{type:"p",text:"\u63A5\u4E0B\u6765\u662F\u670D\u52A1\u5668\u7AEF\u3002\u670D\u52A1\u5668\u5C06\u5728\u4E24\u79CD\u573A\u666F\u4E2D\u4F7F\u7528\u6839\u76EE\u5F55\uFF1A"},{type:"list",ordered:!0,items:["\u6BCF\u5F53\u5DE5\u5177\u5C1D\u8BD5\u8BBF\u95EE\u6587\u4EF6\u6216\u6587\u4EF6\u5939\u65F6",'\u5F53 LLM\uFF08\u5982 Claude\uFF09\u9700\u8981\u5C06\u6587\u4EF6\u6216\u6587\u4EF6\u5939\u89E3\u6790\u4E3A\u5B8C\u6574\u8DEF\u5F84\u65F6\u3002\u60F3\u8C61\u4E00\u4E0B\u5F53\u7528\u6237\u8BF4"\u8BFB\u53D6 todos.txt \u6587\u4EF6"\u65F6\u2014\u2014Claude \u9700\u8981\u5F04\u6E05\u695A\u8BE5\u6587\u672C\u6587\u4EF6\u5728\u54EA\u91CC\uFF0C\u5B83\u53EF\u80FD\u901A\u8FC7\u67E5\u770B\u6839\u76EE\u5F55\u5217\u8868\u6765\u5B9E\u73B0\u8FD9\u4E00\u70B9']},{type:"p",text:"\u4E3A\u4E86\u5904\u7406\u7B2C\u4E8C\u79CD\u60C5\u51B5\uFF0C\u6211\u4EEC\u53EF\u4EE5\u5B9A\u4E49\u4E00\u4E2A\u5217\u51FA\u6839\u76EE\u5F55\u7684\u5DE5\u5177\uFF0C\u6216\u8005\u76F4\u63A5\u5C06\u5B83\u4EEC\u6CE8\u5165\u5230\u63D0\u793A\u4E2D\u3002"}],file:"mcp_server.py",line:49,endLine:58},{title:"\u8BBF\u95EE\u6839\u76EE\u5F55",body:[{type:"p",text:"\u901A\u8FC7\u8C03\u7528 `ctx.session.list_roots()` \u6765\u8BBF\u95EE\u6839\u76EE\u5F55\u3002"},{type:"p",text:"\u8FD9\u4F1A\u5411\u5BA2\u6237\u7AEF\u53D1\u9001\u4E00\u6761\u6D88\u606F\uFF0C\u4ECE\u800C\u4F7F\u5176\u8FD0\u884C\u6839\u76EE\u5F55\u5217\u8868\u56DE\u8C03\u3002"}],file:"mcp_server.py",line:55,endLine:55},{title:"\u6388\u6743\u8BBF\u95EE",body:[{type:"p",text:"\u8BF7\u8BB0\u4F4F\uFF1AMCP SDK \u4E0D\u4F1A\u5C1D\u8BD5\u9650\u5236\u60A8\u7684\u5DE5\u5177\u53EF\u4EE5\u8BFB\u53D6\u54EA\u4E9B\u6587\u4EF6\u6216\u6587\u4EF6\u5939\uFF01\u60A8\u5FC5\u987B\u81EA\u5DF1\u5B9E\u73B0\u8BE5\u68C0\u67E5\u3002"},{type:"p",text:"\u8003\u8651\u5B9E\u73B0\u4E00\u4E2A\u7C7B\u4F3C `is_path_allowed` \u7684\u51FD\u6570\uFF0C\u5B83\u5C06\u901A\u8FC7\u5C06\u8DEF\u5F84\u4E0E\u6839\u76EE\u5F55\u5217\u8868\u8FDB\u884C\u6BD4\u8F83\u6765\u51B3\u5B9A\u8BE5\u8DEF\u5F84\u662F\u5426\u53EF\u8BBF\u95EE\u3002"}],file:"mcp_server.py",line:11,endLine:29},{title:"\u6388\u6743\u8BBF\u95EE",body:[{type:"p",text:"\u4E00\u65E6\u60A8\u6784\u5EFA\u597D\u4E86\u4E00\u4E2A\u6388\u6743\u51FD\u6570\u2014\u2014\u6BD4\u5982 `is_path_allowed`\u2014\u2014\u8BF7\u5728\u60A8\u7684\u5DE5\u5177\u4E2D\u4F7F\u7528\u5B83\uFF0C\u4EE5\u786E\u4FDD\u8BF7\u6C42\u7684\u8DEF\u5F84\u662F\u53EF\u8BBF\u95EE\u7684\u3002"}],file:"mcp_server.py",line:43,endLine:44}]}},p="btn_Ca7V",m="btnPrimary_MXlE",d="btnSecondary_Avja",_="treeItem_AR3b",u="treeName_winm",f=["steps","buttons","editor"],h={py:"python",md:"markdown",json:"json",toml:"toml"};function y(e,t){return e.replace(/\{(\w+)\}/g,(e,s)=>void 0!==t[s]?String(t[s]):`{${s}}`)}function g({text:e}){return e.split(/(`[^`]+`)/g).map((e,t)=>e.startsWith("`")&&e.endsWith("`")&&e.length>=2?(0,o.jsx)("code",{className:"inlineCode_pYO8",children:e.slice(1,-1)},t):(0,o.jsx)(n.Fragment,{children:e},t))}function x({body:e}){return e.map((e,t)=>{if("list"===e.type){let s=e.ordered?"ol":"ul";return(0,o.jsx)(s,{children:e.items.map((e,t)=>(0,o.jsx)("li",{children:(0,o.jsx)(g,{text:e})},t))},t)}return(0,o.jsx)("p",{children:(0,o.jsx)(g,{text:e.text})},t)})}function v({variant:e="sampling"}){let t=c[e];if(!t)throw Error(`McpWalkthrough: unknown variant "${e}"`);return(0,o.jsx)(b,{data:t},e)}function b({data:e}){let{files:t,steps:s}=e,c=(0,a.A)(),g=s[0]?.file??null,[v,C]=(0,n.useState)(0),[P,M]=(0,n.useState)(0),[w,k]=(0,n.useState)(g?[g]:[]),[j,S]=(0,n.useState)(g),[A,N]=(0,n.useState)(s[0]?{file:s[0].file,start:s[0].line,end:s[0].endLine}:null),[E,R]=(0,n.useState)(()=>new Set),[T,L]=(0,n.useState)(0),q=(0,n.useRef)(null),F=(0,n.useMemo)(()=>(function(e){let t={type:"folder",name:"",path:"",children:[]};for(let s of e){let e=s.split("/"),o=t;e.forEach((t,n)=>{if(n===e.length-1)return void o.children.push({type:"file",name:t,path:s});let r=e.slice(0,n+1).join("/"),i=o.children.find(e=>"folder"===e.type&&e.path===r);i||(i={type:"folder",name:t,path:r,children:[]},o.children.push(i)),o=i})}let s=e=>[...e].sort((e,t)=>e.type===t.type?e.name.localeCompare(t.name):"folder"===e.type?-1:1).map(e=>"folder"===e.type?{...e,children:s(e.children)}:e);return s(t.children)})(Object.keys(t)),[t]),D=e=>{let t=s[e];t&&(C(e),M(e),S(t.file),k(e=>e.includes(t.file)?e:[...e,t.file]),N({file:t.file,start:t.line,end:t.endLine}))},I=e=>{S(e),k(t=>t.includes(e)?t:[...t,e]),N(null)};(0,n.useEffect)(()=>{if(!A||j!==A.file)return;let e=q.current;if(!e)return;let t=e.querySelector(`[data-line="${A.start}"]`);if(!t)return;let s=t.offsetTop-e.clientHeight/2+t.offsetHeight/2;e.scrollTo({top:Math.max(s,0),behavior:"smooth"})},[A,j]);let O=null===T?null:f[T],V=e=>O===e?"ring_vq8z":void 0,U=(e,t)=>e.map(e=>{let s={paddingLeft:`${8+14*t}px`};if("file"===e.type){let t=e.path===j;return(0,o.jsxs)("button",{type:"button",onClick:()=>I(e.path),"aria-pressed":t,style:s,className:(0,r.A)(_,t&&"treeItemActive_x2Ro"),children:[(0,o.jsx)("span",{"aria-hidden":"true",children:"\u{1F4C4}"}),(0,o.jsx)("span",{className:u,children:e.name})]},e.path)}let n=E.has(e.path);return(0,o.jsxs)("div",{children:[(0,o.jsxs)("button",{type:"button","aria-expanded":!n,onClick:()=>{var t;return t=e.path,void R(e=>{let s=new Set(e);return s.has(t)?s.delete(t):s.add(t),s})},style:s,className:_,children:[(0,o.jsx)("span",{"aria-hidden":"true",children:n?"\u{1F4C1}":"\u{1F4C2}"}),(0,o.jsx)("span",{className:u,children:e.name})]}),!n&&U(e.children,t+1)]},e.path)}),z=null===j?void 0:t[j],H=c.plain.backgroundColor;return(0,o.jsxs)("div",{className:"root_PdEE",children:[null!==T&&(0,o.jsxs)("div",{className:"tour_3RlD",children:[(0,o.jsx)("div",{className:"tourDots_TuWE","aria-label":y(l["tour.aria"],{n:T+1,total:f.length}),children:f.map((e,t)=>(0,o.jsx)("span",{"aria-hidden":"true",className:(0,r.A)("dot_y8Dq",t===T&&"dotActive_XW_U")},e))}),(0,o.jsx)("h3",{className:"tourTitle_CLQ9",children:l[`tour.${f[T]}.title`]}),(0,o.jsx)("p",{className:"tourBody_hdmF",children:l[`tour.${f[T]}.body`]}),(0,o.jsxs)("div",{className:"tourActions_lWDH",children:[T>0?(0,o.jsx)("button",{type:"button",onClick:()=>L(T-1),className:(0,r.A)(p,d),children:l["tour.btn.prev"]}):(0,o.jsx)("button",{type:"button",onClick:()=>L(null),className:(0,r.A)(p,d),children:l["tour.btn.skip"]}),T<f.length-1?(0,o.jsx)("button",{type:"button",onClick:()=>L(T+1),className:(0,r.A)(p,m),children:l["tour.btn.next"]}):(0,o.jsx)("button",{type:"button",onClick:()=>L(null),className:(0,r.A)(p,m),children:l["tour.btn.finish"]})]})]}),(0,o.jsxs)("div",{className:"layout_Rpv_",children:[(0,o.jsxs)("div",{className:"stepsPanel_w37F",children:[(0,o.jsx)("div",{className:(0,r.A)("stepList_L6g4",V("steps")),children:s.map((e,t)=>{let s=P===t,n=v===t,i=`mcp-walkthrough-step-${t}`;return(0,o.jsxs)("div",{className:"step_ewox",children:[(0,o.jsxs)("button",{type:"button","aria-expanded":s,"aria-controls":i,onClick:()=>s?M(null):D(t),className:(0,r.A)("stepHeader_ZbAq",n&&"stepHeaderActive_omTS"),children:[(0,o.jsx)("span",{children:y(l["step.heading"],{n:t+1,title:e.title})}),(0,o.jsx)("span",{"aria-hidden":"true",className:(0,r.A)("chevron_QyLX",s&&"chevronOpen_Cn88"),children:"\u25B6"})]}),(0,o.jsx)("div",{id:i,hidden:!s,className:"stepBody_tsuF",children:(0,o.jsx)(x,{body:e.body})})]},t)})}),(0,o.jsxs)("div",{className:(0,r.A)("navButtons_qQm5",V("buttons")),children:[(0,o.jsx)("button",{type:"button",disabled:0===v,onClick:()=>D(v-1),className:(0,r.A)(p,m),children:l["btn.prev"]}),(0,o.jsx)("button",{type:"button",disabled:v===s.length-1,onClick:()=>D(v+1),className:(0,r.A)(p,m),children:l["btn.next"]})]})]}),(0,o.jsxs)("div",{className:(0,r.A)("editor_AUWM",V("editor")),children:[(0,o.jsxs)("aside",{className:"fileTree_Tk7t",children:[(0,o.jsx)("h4",{className:"fileTreeHeading_HuW5",children:l["files.heading"]}),(0,o.jsx)("div",{className:"treeScroll_wply",children:U(F,0)})]}),(0,o.jsxs)("div",{className:"editorMain_CDJJ",children:[(0,o.jsx)("div",{className:"tabs_I7zQ","aria-label":l["aria.openFiles"],children:w.map(e=>{let t=e===j;return(0,o.jsxs)("div",{className:(0,r.A)("tab_UQJu",t&&"tabActive_baSF"),style:t?{backgroundColor:H}:void 0,children:[(0,o.jsx)("button",{type:"button","aria-pressed":t,onClick:()=>I(e),className:"tabBtn_Sclk",children:e.split("/").pop()}),(0,o.jsx)("button",{type:"button","aria-label":y(l["aria.closeTab"],{path:e}),onClick:()=>{let t;k(t=w.filter(t=>t!==e)),j===e&&S(t.length>0?t[t.length-1]:null)},className:"tabClose_zVNs",children:"\xd7"})]},e)})}),null!==j&&void 0!==z?(0,o.jsx)("div",{ref:q,className:"codeScroll_blyh",style:{backgroundColor:H},children:(0,o.jsx)(i.f4,{theme:c,code:z.replace(/\n$/,""),language:h[j.split(".").pop()]||"text",children:({tokens:e,getLineProps:t,getTokenProps:s})=>(0,o.jsx)("pre",{className:"pre_TtiE",style:{color:c.plain.color},children:e.map((e,n)=>{let i=n+1,a=null!==A&&A.file===j&&i>=A.start&&i<=A.end,l=t({line:e});return(0,o.jsxs)("div",{...l,"data-line":i,className:(0,r.A)(l.className,"line_Wrfh",a&&"lineHl_JKl4"),children:[(0,o.jsx)("span",{"aria-hidden":"true",className:"lineNo_qn6s",children:i}),(0,o.jsx)("span",{className:"lineContent_EJ3S",children:e.map((e,t)=>(0,o.jsx)("span",{...s({token:e})},t))})]},i)})})})}):(0,o.jsx)("div",{className:"empty_y7un",style:{backgroundColor:H},children:(0,o.jsxs)("div",{children:[(0,o.jsx)("p",{children:l["empty.title"]}),(0,o.jsx)("p",{className:"emptySub_SbYa",children:l["empty.body"]})]})})]})]})]})]})}},8453(e,t,s){s.d(t,{R:()=>i,x:()=>a});var o=s(6540);let n={},r=o.createContext(n);function i(e){let t=o.useContext(r);return o.useMemo(function(){return"function"==typeof e?e(t):{...t,...e}},[t,e])}function a(e){let t;return t=e.disableParentContext?"function"==typeof e.components?e.components(n):e.components||n:i(e.components),o.createElement(r.Provider,{value:t},e.children)}}}]);