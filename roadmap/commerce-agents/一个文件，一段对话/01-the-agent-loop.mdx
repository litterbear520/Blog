---
description: 以商品搜索为例，理解模型如何请求工具，以及程序为什么要把结果送回对话。
---

import CodeBlock from '@theme/CodeBlock';
import AgentLoopViz from '@site/src/components/AgentLoopViz';
import CodeWalkthrough from '@site/src/components/CodeWalkthrough';
import { snippets } from '@site/src/data/commerceAgentLoopLesson';

# 循环与第一个工具

工具让模型能够取得对话之外的信息。以购物助手为例，我们可以提供一个商品搜索工具，让模型先查询商品目录，再根据结果回答用户。

## 为什么需要工具

假设用户问：“店里有没有降噪耳机？”模型可以介绍降噪耳机，却无法仅凭已有知识确认这家店的商品和库存。要回答这个问题，它需要取得商店的数据。

我们可以让程序查询商品目录，再把结果交给模型。这里有一个重要的分工：**模型决定需要查询什么，程序负责执行查询，模型再根据结果回答。** 模型发出工具调用请求，并不代表 Python 函数已经执行。

## 定义商品搜索

先为程序准备一个普通的 Python 函数。为了专注于工具调用，我们用内存中的 `PRODUCTS` 列表代替数据库，按商品名称匹配关键词：

<CodeBlock language="python" title="搜索函数（节选）">{snippets.search}</CodeBlock>

调用 `search_products("耳机")`，就会找到名称中包含“耳机”的商品，并把结果转换为 JSON 字符串。此时，搜索已经能在 Python 中独立工作，但模型还不知道怎样请求它。

我们还需要一份工具定义，也就是 Schema。它通过 `name` 标识工具，通过 `description` 说明用途，再用 `input_schema` 描述参数。在这个例子中，`query` 是必填的关键词，`limit` 是可选的结果数量。

<details>
<summary>查看搜索工具的定义</summary>

<CodeBlock language="python" title="工具 Schema">{snippets.schema}</CodeBlock>

</details>

把这份定义放进请求的 `tools=[search_products_schema]` 后，模型就可以提出搜索请求。例如，针对用户的问题，它可以选择 `search_products`，并提供参数 `{"query": "耳机"}`。**工具定义描述调用方式，函数负责实际搜索。**

## 执行工具并返回结果

模型需要搜索时，回复中会出现 `tool_use` 内容块，包含工具名称、参数和调用 ID。程序先把完整的 `response.content` 作为 `assistant` 消息追加到 `messages`，再处理其中的工具调用。

下面的代码用 `TOOL_MAP` 按名称找到搜索函数，执行后把结果包装成 `tool_result`：

<CodeBlock language="python" title="执行工具并追加结果（节选）">{snippets.results}</CodeBlock>

`tool_use_id` 对应这次请求的 `block.id`，用来说明“这是哪一次调用的结果”，而不是搜索到的商品 ID。结果放在一条 `user` 消息中；这里的 `user` 是 Messages API 的角色，并不表示用户又输入了一句话。

只有把这些消息带入下一次模型请求，模型才能读到搜索结果。仅在终端打印结果，并不会把数据交给模型。

## 为什么需要循环

第一次请求让模型提出搜索，下一次请求才让它读到结果。如果信息还不够，模型也可以继续请求工具。因此，程序需要重复“请求模型—执行工具—回传结果”，而不是调用一次 API 就结束。

模型回复中的 `stop_reason` 用来区分这些情况：`tool_use` 表示需要执行工具并继续；`end_turn` 表示当前这一轮回答正常结束。示例中的内层循环用下面的条件决定是否继续：

<CodeBlock language="python" title="循环中的判断（节选）">{snippets.stop}</CodeBlock>

这段最小实现遇到非 `tool_use` 的情况都会退出；实际应用还需要单独处理 `max_tokens` 等停止原因，不能把退出都当成正常完成。

下面用一次找耳机的过程串起这些步骤。留意搜索结束后，程序为什么还要请求一次模型，以及 `messages` 中增加了哪些消息。演示是预设过程的回放，不会发送真实 API 请求。

<AgentLoopViz variant="commerceLoop" />

完整脚本的外层循环负责等待用户输入，内层循环负责完成当前问题所需的工具调用。外层保留 `messages`，所以用户继续追问时，模型仍能看到前面的对话。

<details>
<summary>查看完整代码与运行方式</summary>

下面的演练默认定位到完整循环，也可以回看搜索函数和工具定义。正文中的代码节选来自同一份快照。

<CodeWalkthrough variant="commerce" step={5} nav />

将最终版本保存为 `agent.py`，在虚拟环境中安装依赖：

```bash
python -m pip install anthropic python-dotenv
```

当前代码快照使用 DeepSeek 的 Anthropic 兼容接口。在本地 `.env` 中配置该服务的 `ANTHROPIC_API_KEY`，不要把真实密钥提交到仓库；使用其他服务时，需要一并核对接口地址、模型名和密钥。

```bash
python agent.py
```

输入“我想找耳机”，观察搜索结果如何进入后续回答。也可以输入目录中不存在的“望远镜”，观察模型怎样处理空结果。真实模型的措辞和调用次数可能不同，这里关注的是调用与回传的过程。

</details>

这就是购物助手的基本循环：模型提出工具请求，程序执行并送回结果，模型继续回答。以后加入商品详情和购物车时，我们仍会沿用这个过程，只是让模型可以选择更多工具。

<details>
<summary>参考资料</summary>

[工具调用入门 · Claude Academy](https://academy.claude.com/courses/building-with-the-claude-api/introducing-tool-use) · [处理工具调用与结果 · Claude API 文档](https://platform.claude.com/docs/en/agents-and-tools/tool-use/handle-tool-calls)

</details>
